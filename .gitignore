node_modules/
dist/
# Local secrets and environment-specific configuration. Keep only checked-in
# `.env.example` templates so credentials and endpoints never enter Git.
.env
.env.*
!.env.example
.DS_Store
.idea/

# WeChat DevTools stores per-developer preferences here.
miniprogram/project.private.config.json

venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Python 依赖安装目录（本地构建产物）
smart-kitchen-ai/.deps/

# AI 服务运行后产生的缓存、WAL 与索引不纳入版本控制。
# The database can contain cached prompts and knowledge-base data; keep it local.
smart-kitchen-ai/data/milvus.db/

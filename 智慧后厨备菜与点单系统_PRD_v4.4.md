# 智慧后厨备菜与点单系统 产品需求文档（PRD）

> 版本：v5.2
> 说明：接口级技术细节见《技术规格说明书》（spec.md），迭代过程见《开发计划》（plan.md）。

---

## 修订记录


| 版本        | 日期                | 修订内容                                                                                     |
| --------- | ----------------- | ---------------------------------------------------------------------------------------- |
| v1.0–v4.7 | 2026-07 ~ 2026-08 | 开发过程中的迭代版本：明确技术选型、确立「先做后付」业务模式、简化状态机、确定 LangGraph 单工作流架构、补充前端范围                          |
| v5.0      | 2026-08-17        | 加菜改为父子订单模型、结账改为「先登记后流转」、WebSocket 事件模型按实际推送口径修订、知识库改为文本录入 + 文件解析上传、预测工作流按并行图结构修订、删除过程性内容 |
| v5.1      | 2026-08-17        | 订单状态迁移改为「应用层预校验 + SQL 条件更新最终裁决」；WebSocket 握手增加 JWT 鉴权（看板限 ADMIN，顾客 userId 以 token 解析为准）；MQ 消费异常口径修订为 Nack 进兜底 DLQ |
| v5.2      | 2026-08-24        | 认证加固：管理员密码改为 BCrypt；HTTP 拦截器对管理端接口强制 ADMIN（403）；Access/Refresh 双 Token + Redis 黑名单/刷新旋转/全端吊销 |
| v5.3      | 2026-09-01        | 业务模式改为「先付后做」：下单不推厨房看板，15 分钟支付窗口内支付成功才上厨；超时下由 RabbitMQ 延迟消息（x-delayed-message 插件）+ 2 分钟扫表兜底自动取消还库存；发送可靠性改为 confirm 异步重试 + 失败记录表，不设本地消息表 |


---

## 一、项目概述

### 1.1 背景与目标

本项目为中型餐饮门店提供一套数字化点单与后厨管理系统，针对三个经营痛点给出工程化解决方案：


| 痛点              | 解决方案                                                             |
| --------------- | ---------------------------------------------------------------- |
| 高峰期并发下单导致库存超卖   | Redis + Lua 脚本原子化库存扣减，配合数据库二次校验                                  |
| 凭经验备菜导致食材损耗     | LangGraph 多节点工作流，融合销量、天气、节假日、顾客评价四维数据预测备菜量                       |
| 顾客咨询占用人力、信息获取低效 | AI 智能客服（ReAct Agent + Function Calling + RAG 知识库），提供菜品推荐、库存与配料查询 |




### 1.2 业务模式：先付后做

顾客下单后有 **15 分钟支付窗口**：下单即锁库存但不推厨房，支付成功后订单才上厨制作；窗口内未支付的订单由 RabbitMQ 延迟消息（扫表兜底）自动取消并归还库存。已支付订单出餐即完成（自动流转 `PAID`）。支付为模拟实现（生成模拟流水号），系统设计保留了接入真实支付渠道的扩展点。

### 1.3 范围约定


| 维度    | 约定                                     |
| ----- | -------------------------------------- |
| 门店    | 单门店                                    |
| 用餐类型  | 堂食，固定 10 个座位（A01–A05、B01–B05），下单时选择座位号 |
| 支付    | 模拟支付（Mock），不调真实支付渠道                    |
| 库存粒度  | 菜品级日库存                                 |
| B 端   | 一套 PC Web 管理端（含厨房看板页面）                 |
| B 端用户 | 单管理员，不设多员工/多角色权限体系                     |
| 后厨接单  | 无需接单动作，待制作订单自动出现在厨房看板                  |




### 1.4 系统组成


| 子系统                | 技术                                     | 职责                                    |
| ------------------ | -------------------------------------- | ------------------------------------- |
| `smart-kitchen`    | Java 21 + Spring Boot 3.2              | 鉴权、菜品/订单/库存/评价等业务接口、WebSocket 推送、消息队列 |
| `smart-kitchen-ai` | Python + FastAPI + LangGraph           | AI 客服 Agent、备菜预测工作流、RAG 文档向量化与检索      |
| `admin-web`        | Vue 3 + Element Plus                   | B 端管理后台（PC Web）                       |
| `miniprogram`      | 微信原生小程序                                | C 端顾客点单                               |
| 中间件                | MySQL / Redis / RabbitMQ / Milvus Lite | 存储、缓存、延迟消息、向量检索                       |


---



## 二、用户角色


| 角色         | 接入端                    | 核心能力                                                       |
| ---------- | ---------------------- | ---------------------------------------------------------- |
| 顾客（C 端）    | 微信小程序（`wx.login` 免密登录） | 浏览菜单、选座下单、15 分钟内支付、支付后加菜、查看订单状态、餐后评价、AI 客服咨询                      |
| 餐厅管理员（B 端） | PC Web（账密登录）           | 经营仪表盘、厨房看板出餐、订单管理（撤销）、菜品与分类管理、库存管理、AI 备菜预测触发与确认、知识库维护、评价查看 |


---



## 三、功能模块设计



### 模块 1：顾客点单与订单流转



#### 1.1 下单

**前置条件**：用户已登录（JWT），购物车不为空。

**流程**：

1. 顾客浏览菜单（按分类筛选），将菜品加入购物车（小程序本地存储）。
2. 进入确认订单页：从座位列表选择座位号（已被占用座位置灰不可选），可填写整单备注。
3. 提交订单，后端依次执行：
  - 逐菜品校验（菜品不存在的直接报错；**已下架菜品不拦截**：不计入金额、不扣库存，明细名称标记「（已下架）」）；
  - **Redis + Lua 原子扣减库存**（脚本内：key 不存在则用传入的 `daily_stock` 初值初始化再扣；先全量校验、全部通过后才扣减，任一菜品库存不足则整体失败并返回具体菜品）；
  - 扣减成功后落库生成订单（状态 `ORDERED`），并同步扣减 MySQL 库存（带库存余量条件的 UPDATE，作为第二道防线）；
  - 任一环节失败则执行 Redis 回滚脚本，保证 Redis 与 MySQL 库存一致。
4. 下单成功后（**先付后做**）：
  - 小程序跳转订单详情页引导支付，展示 15 分钟支付倒计时；
  - 事务提交后向 RabbitMQ 延迟交换机（`rabbitmq_delayed_message_exchange` 插件）发送消息，**15 分钟未支付的订单自动取消并归还库存**（MQ 异常时有 2 分钟扫表兜底）；
  - **下单不推厨房看板**，支付成功后才推送新订单卡片。

**库存初始化**：Redis 中库存 key 不存在时，由 `deduct_stock.lua` 在同一段脚本内以菜品表 `daily_stock` 为初值原子写入并扣减（不下沉到 Java 的 `hasKey` + `set`，避免并发双写超卖）。

#### 1.2 加菜

- **入口**：先付后做——**父订单已支付**（`pay_time` 非空）后，订单详情页才显示「加菜」按钮；未支付订单只能支付。
- **实现模型**：加菜**不是在原订单上追加明细，而是创建一张关联原订单的子订单**（`parent_order_id` 指向原订单）：
  - 子订单有独立订单号，独立执行库存扣减（Redis Lua 预扣 + MySQL 条件扣减），**子订单同样有 15 分钟支付窗口**（事务提交后发自己的延迟消息），**未支付不上厨**，支付成功后才作为一张新卡片推送到厨房看板；
  - 顾客端展示时，父子订单合并呈现（合并菜品明细、合并总金额、按规则合并状态，见 1.5 节）。
- **设计收益**：厨房看板无需处理「卡片内容变更」，每张卡片对应一次已支付的制作任务；支付时主订单自动合并支付全部未支付子订单（父单已付时可单独补付子单）。



#### 1.3 支付（模拟支付，先付后做）

- **入口**：`ORDERED` 未支付时，订单详情页显示「支付」按钮（未支付订单只有支付一个操作）；已支付但有未支付子单时仍显示「支付」用于补付子单。
- **流程**：
  1. 点击支付 → 后端登记支付信息：模拟流水号（`SIM_` + 时间戳）+ 支付时间，同时自动合并支付该单的全部未支付加菜子订单（流水号追加 `_1`、`_2` 后缀）；
  2. **支付成功才推厨房看板**：本次支付的 `ORDERED` 单（含补付子单）各自推送 `NEW_ORDER` 卡片；
  3. 状态流转采用「**先登记、后流转**」：
    - `ORDERED`（菜未上齐）→ 状态保持 `ORDERED`，厨房开始制作，待厨房点击「完成出餐」时系统检测到已登记支付，自动流转为 `PAID`；
    - `SERVED`（存量已出餐未结账单）→ 立即变为 `PAID`；
  4. 已登记支付且无未支付子单的订单不再显示「支付」按钮，不可重复支付。
- **幂等**：`payment_trade_no` 唯一索引 + 支付时间非空校验（SQL 条件更新兜底），双重防重。



#### 1.4 撤销与超时取消


| 触发方式                      | 规则                                                 |
| ------------------------- | -------------------------------------------------- |
| 管理员在订单管理页撤销               | `ORDERED` / `SERVED` → `CANCELLED`；级联撤销全部加菜子订单     |
| 支付超时（15 分钟未支付）           | RabbitMQ 延迟消息（x-delayed-message 插件）到期触发，另有 2 分钟扫表兜底；**仅 `ORDERED` 且未支付执行取消**（原因 `PAY_TIMEOUT`），已支付/已撤销的订单直接跳过，**SERVED 永不超时取消**；父单超时可级联未支付子单，子单超时只取消自己；与支付并发时只成功一笔（SQL 条件更新裁决） |


**库存返还规则**：仅未出餐（`ORDERED`）的订单撤销时返还库存——先 MySQL 原子加回，再同步 Redis（key 存在则累加返还量；key 已被管理员删除则直接写回返还后的 DB 库存，避免从 0 起跳）；已出餐（`SERVED`）的订单食材已消耗，撤销不返还库存。

#### 1.5 订单状态机

```
ORDERED(0) 已下单（未支付，15 分钟支付窗口）
  ├── 顾客[支付] ──→ 登记支付信息，状态保持 ORDERED，推厨房看板（先付后做）
  │     ├── 厨房[完成出餐] ──→ 已有 pay_time ──→ PAID(20) 已结账
  │     ├── 顾客[加菜] ──→ 生成新的 ORDERED 子订单（同样有 15 分钟支付窗口，付完才上厨）
  │     └── 管理员[撤销] ──→ CANCELLED(90)
  ├── 支付超时（MQ 延迟消息 / 扫表兜底）──→ CANCELLED(90)，原因 PAY_TIMEOUT，返还库存
  └── 管理员[撤销] ──→ CANCELLED(90)
```

**状态码定义**：


| 状态码 | 状态        | 顾客端可见按钮           | 厨房看板        |
| --- | --------- | ----------------- | ----------- |
| 0   | ORDERED   | 未支付：支付；已支付：加菜（有未支付子单仍含支付） | **仅已支付**显示卡片 + 完成按钮 |
| 10  | SERVED    | 加菜、支付（存量兼容）   | 不显示         |
| 20  | PAID      | 评价（未评价时） | 不显示         |
| 90  | CANCELLED | 无        | 不显示         |


**父子订单合并状态规则**（C 端展示用）：

- 任一子订单被取消 → 整体显示 `CANCELLED`；
- 全部订单均为 `PAID` → 整体显示 `PAID`；
- 全部订单均至少 `SERVED` → 整体显示 `SERVED`；
- 其余情况 → 整体显示 `ORDERED`。

**按钮可用性由后端计算下发**：订单详情接口返回 `availableActions` 字段（`ADD_DISH` / `PAY` / `REVIEW`），前端按字段渲染按钮，不在前端重复实现状态规则。

#### 1.6 顾客端页面


| 页面         | 功能                                                |
| ---------- | ------------------------------------------------- |
| 菜单（Tab）    | 分类 Tab 筛选、菜品卡片（图片/名称/价格/库存状态）、购物车弹层（增删改数量）、加菜模式入口 |
| 菜品详情       | 大图、价格、库存、配料与过敏原信息、加购按钮；库存为 0 不可加购                 |
| 确认订单       | 可视化座位选择（占用置灰）、购物车清单校验（下架菜品灰显不计价）、备注、提交            |
| 订单详情       | 状态展示、未支付 15 分钟支付倒计时（到期自动刷新）、合并后的菜品明细、按 `availableActions` 动态显示支付/加菜/评价按钮 |
| 我的订单（Tab）  | 历史订单分页列表，上拉加载更多                                   |
| 评价         | 五星评分 + 文字评价（选填）                                   |
| AI 客服（Tab） | 对话式咨询，流式打字机效果输出                                   |


---



### 模块 2：管理端（B 端 PC Web）



#### 2.1 仪表盘首页

管理端默认首页，四张统计卡片：**今日订单数**（非撤销订单）、**今日营收**（`PAID` 订单金额合计）、**待出餐数量**（当前 `ORDERED` 订单数）、**库存预警**（库存 ≤ 预警阈值的菜品数）。进入页面时请求一次，非实时推送。

#### 2.2 厨房看板

- **定位**：后厨制作任务看板，大字号卡片式布局，适合挂屏。
- **数据源**：所有 `ORDERED` 状态订单（含加菜子订单），按下单时间升序，一单一卡。
- **卡片信息**：座位号、下单时间、菜品明细（菜名 × 数量）、备注。
- **操作**：「完成出餐」→ 已支付订单 `ORDERED` → `PAID`（先付后做，看板只展示已支付单），卡片消失，同时通过 WebSocket 通知对应顾客「您的订单已出餐」。
- **实时更新**：
  - WebSocket 接收 `NEW_ORDER` 事件（新下单、加菜子订单）→ 新增卡片；
  - 断线自动重连，重连成功后拉取一次 HTTP 全量快照补齐数据；
  - 出餐、撤销由管理员在本页面或订单管理页操作，操作成功后前端本地移除卡片。



#### 2.3 订单管理

- 全量订单分页列表（时间倒序），支持按状态、座位号筛选。
- 每行展示：座位号、菜品明细、金额、状态、时间；点击「详情」弹窗查看完整明细。
- 操作：「完成出餐」（`ORDERED`）、「撤销」（`ORDERED` / `SERVED`，终态不可操作）。



#### 2.4 菜品与分类管理

- **菜品**：列表（支持按分类筛选）、新增/编辑/删除、上下架开关、图片上传（经后端代理转存对象存储，返回图片 URL 回显）。
- **分类**：分类列表、新增、删除（内联操作）。
- 菜品维护字段：名称、分类、价格、图片、日库存、预警阈值、配料、过敏原、新品初始库存（供 AI 预测冷启动使用）。



#### 2.5 库存管理

- 菜品库存列表，库存低于预警阈值的行高亮警示。
- 点选菜品查看当前库存与**变更流水**（管理员手动调整的记录：变更量、变更前后值、时间）。
- 手动调整库存（盘点、损耗登记等场景），调整后写库存流水。



#### 2.6 评价管理

评价只读列表（订单号、星级、内容、时间），支持按评分筛选。评价数据同时作为 AI 备菜预测的输入维度（见模块 3）。

---



### 模块 3：AI 备菜预测（LangGraph 多节点工作流）



#### 3.1 工作流结构

预测流程为单一 LangGraph 工作流（`predict_subgraph`），由定时任务或触发接口直接调用，无 Supervisor 路由层。图中仅 `llm_adjust` 一个节点调用大模型，其余节点均为确定性函数，成本与时延可控。

```
                    ┌─→ get_sales_30d（近30天日销量，MySQL）──→ time_series_predict ─┐
START（每道在售菜品） ┼─→ get_tomorrow_weather（目标日天气）  ────────────────────────┼─→ llm_adjust ─→ save_result → END
                    ├─→ get_holiday_info（是否节假日/周末） ────────────────────────┤
                    └─→ get_recent_reviews（近期评价与均分，MySQL）─────────────────┘
```

四个数据采集节点并行执行；`time_series_predict` 依赖销量数据；`llm_adjust` 等待全部前置节点完成后执行。

#### 3.2 节点说明


| 节点                     | 类型     | 说明                                                    |
| ---------------------- | ------ | ----------------------------------------------------- |
| `get_sales_30d`        | 函数     | MySQL 汇总历史日销量（营业日口径：营业未售记 0、休息日/上架前剔除；窗口为昨天往前 30 天；仅取与目标日同类型的周末/工作日） |
| `get_tomorrow_weather` | 外部 API | OpenWeatherMap 5 日预报（3 小时间隔），聚合为目标日级数据（温度、天气状况、风力、湿度）；与菜品无关，每次任务仅拉取一次注入各菜品 |
| `get_holiday_info`     | MCP 调用 | 通过标准 MCP JSON-RPC 协议调用节假日服务，判断目标日是否节假日/周末；同样按任务仅拉取一次     |
| `get_recent_reviews`   | 函数     | MySQL 查询近期评价明细与平均评分                                   |
| `time_series_predict`  | 函数     | 基于同类型营业日销量的统计量（均值/中位数/近 7 个同类型日均值）计算基础预测量                  |
| `llm_adjust`           | LLM    | 组装 Prompt（基础预测 + 天气 + 节假日 + 评价）调大模型，输出结构化 JSON 修正建议   |
| `save_result`          | 函数     | 预测结果 upsert 写入 `ai_prediction_record` 表（按日期 + 菜品唯一）   |


LLM 输出 Schema：

```json
{
  "dish_name": "烤鱼",
  "base_quantity": 50,
  "final_quantity": 65,
  "reasoning": "近期评分4.9、明日暴雨降温预计客流增加，建议上调备货",
  "confidence": 0.85
}
```



#### 3.3 冷启动与降级策略

核心原则：**预测任务绝不因单点故障中断**。

**冷启动逐级降级**（新店/新品无历史销量时）：


| 级别  | 条件             | 基础预测量取值             |
| --- | -------------- | ------------------- |
| 0   | 近 30 天同类型营业日 ≥ 7 天 | 同类型营业日销量统计均值      |
| 1   | 同类型营业日不足 7 天     | 近 N 个同类型营业日均值     |
| 2   | 无销量            | 同分类菜品近 30 天日均销量     |
| 3   | 同分类也无数据        | 菜品的新品初始库存（管理员上架时设定） |
| 4   | 未设新品初始库存       | 菜品的日库存              |
| 5   | 以上全无           | 绝对兜底值 20            |


**LLM 降级**：LLM 调用超时（60s）或返回格式非法时，直接采用时序预测结果作为最终建议量，置信度标记为 0.5，推理说明注明降级原因。

**外部 API 降级**：天气/节假日服务不可用时跳过该维度，Prompt 中注明数据缺失，流程继续。

#### 3.4 触发与人工确认

- **自动触发**：每日 02:00 定时任务，预测次日全部在售菜品。
- **手动触发**：管理员在预测页面选择目标日期触发；任务异步执行并返回任务 ID，前端轮询任务进度。
- **结果展示**：菜品、基础预测量、AI 建议量、近期评分、推理说明、置信度；低置信度（< 0.4）的行高亮标记。
- **人工确认**：管理员可修改最终确认量并提交，记录确认人；建议采购量 = max(0, 最终确认量 − 当前库存)。



#### 3.5 评价反馈闭环

顾客餐后评价（模块 4.4）写入 `oms_review` 表 → 预测工作流的 `get_recent_reviews` 节点读取 → 作为 LLM 修正依据之一。系统由此感知菜品口碑变化对销量的影响，形成「经营数据 → 预测 → 备菜 → 经营」的数据闭环。

---



### 模块 4：AI 智能客服与 RAG 知识库



#### 4.1 智能客服（单 Agent + Function Calling）

C 端小程序「AI 客服」Tab，基于 LangGraph ReAct Agent，挂载 3 个工具函数：


| 工具                          | 数据来源              | 说明                       |
| --------------------------- | ----------------- | ------------------------ |
| `search_dish_by_preference` | Milvus 知识库        | 按口味/人群/场景等模糊描述做语义检索，推荐菜品 |
| `check_dish_inventory`      | Java 代理接口 → MySQL | 实时库存查询                   |
| `get_dish_ingredients`      | Java 代理接口 → MySQL | 配料与过敏原查询                 |


**数据分工原则**：结构化事实（库存、配料、价格）走数据库精确查询；开放性问题（推荐、搭配）走 RAG 语义检索；过敏原相关回答以工具返回的硬数据为准，不依赖模型自由发挥。

**回答约束**（System Prompt 层）：单轮最多 3 次工具调用、同一工具单轮最多 1 次；工具无结果时给出固定话术引导，不编造。

**流式输出**：SSE（Server-Sent Events）逐块推送，小程序端以 `enableChunked` 分块接收，呈打字机效果；低版本基础库降级为普通 POST 整包返回。

**语义缓存**：问题先转 embedding，与 Milvus 集合 `ai_chat_semantic_cache` 中的历史问题向量算余弦相似度，≥ 0.92 即命中并直接返回缓存回答（按固定切片模拟流式，不再调用 LLM）；近义问法（差字、换语序）也能命中，命中率远高于原 MD5 精确匹配。**只缓存非动态回答**——调用了库存/配料等实时工具的回答不写入缓存，避免向其他用户散发过期数据；缓存条目带知识库版本指纹，知识库元数据写入成功后主动失效指纹缓存（并保留短 TTL 兜底），旧缓存自动失效。

#### 4.2 知识库管理（B 端维护 → C 端使用）

- **录入方式**（管理端「AI 知识库」页）：
  1. 直接填写标题与文本内容；
  2. 上传 Word / PDF 文件，由 Java 端解析为纯文本后自动填充，再确认提交。
- **处理流水线**：文本 → `RecursiveCharacterTextSplitter` 分块（chunk_size=500，overlap=50）→ Embedding（1024 维向量）→ 写入 Milvus 集合 `kitchen_knowledge`。
- **版本管理**：每次提交生成独立的文档 ID，携带版本号、状态（draft/active/archived）、生效时间；检索时可按版本过滤，新旧版本并存互不影响。
- **元数据**：文档标题、分块数、版本、状态等元数据落 MySQL `ai_knowledge_document` 表，管理端列表展示。
- **内容定位**：知识库存放口味描述、适用人群、推荐搭配等「软知识」；价格、库存、配料等易变事实不进知识库，由工具实时查询，避免知识过期。



#### 4.3 餐后评价

- **入口**：`PAID` 状态订单的详情页显示「评价」按钮（一单一评，已评价后按钮消失）。
- **内容**：1–5 星评分 + 可选文字评价。
- **用途**：管理端评价列表查看；评价数据流入备菜预测工作流（模块 3.5）。

---



## 四、核心数据模型

数据库共 9 张表。以下为概念模型，完整 DDL 与索引定义见 spec.md 第 5 章。


| 表                       | 说明             | 关键字段                                                                                                                  |
| ----------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------- |
| `sys_user`              | 系统用户（顾客 + 管理员） | openid（微信）、username、role（CUSTOMER/ADMIN）                                                                              |
| `pms_category`          | 菜品分类           | name、sort                                                                                                             |
| `pms_dish`              | 菜品             | price、status（起售/停售）、daily_stock、alert_threshold、ingredients、allergens、new_product_initial_stock                       |
| `oms_order`             | 订单主表           | order_no、seat_number、total_amount、status、parent_order_id（加菜子订单）、payment_trade_no、pay_time、complete_time、cancel_reason |
| `oms_order_detail`      | 订单明细           | dish_name / price 快照、quantity、is_added（首单/加菜）                                                                         |
| `oms_review`            | 餐后评价           | order_id（唯一，一单一评）、score、comment                                                                                       |
| `inv_stock_log`         | 库存变更流水         | change_type、change_qty、before_qty、after_qty                                                                           |
| `ai_prediction_record`  | 备菜预测记录         | predict_date + dish_id（唯一）、base_quantity、ai_suggest_quantity、final_quantity、reasoning、confidence、status、confirmed_by  |
| `ai_knowledge_document` | 知识库文档元数据       | title、chunk_count、version、status、effective_from                                                                       |


**设计要点**：

- 订单明细冗余菜品名称与价格快照，菜品改价/删除不影响历史订单。
- 加菜子订单与原订单同表存储，通过 `parent_order_id` 关联；顾客端查询仅取主订单，展示时再合并子订单。
- 座位不落表：固定 10 个座位，占用状态由「当前 `ORDERED` / `SERVED` 订单的座位号」实时推导。
- 支付流水号唯一索引，从数据库层面保证支付幂等。

---



## 五、非功能性需求



### 5.1 性能


| 指标        | 目标      | 手段                        |
| --------- | ------- | ------------------------- |
| 下单/加菜接口   | 高并发下不超卖 | Redis Lua 原子扣减，库存热点数据常驻内存 |
| 核心接口响应    | < 200ms | 库存走 Redis，MySQL 仅做最终一致落库  |
| AI 客服首字延迟 | 秒级      | SSE 流式输出 + 热点问题缓存         |
| 厨房看板订单上屏  | 实时      | WebSocket 推送，支付成功即推（先付后做）      |




### 5.2 数据一致性

- **库存**：Redis Lua 预扣（脚本内原子初始化缺失 key）+ MySQL 条件更新双写；失败路径执行 Redis 回滚脚本；撤销时先改 MySQL 再改 Redis（key 缺失写回 DB 真值）；菜品下架期间自动跳过库存操作。
- **支付幂等**：`payment_trade_no` 唯一索引 + `pay_time` 非空校验 + 支付登记 SQL 条件更新（`pay_time IS NULL` 才能写入）三重保障。
- **订单状态**：「应用层前置校验 + SQL 条件更新最终裁决」——状态迁移以 `WHERE id=? AND status IN (...)` 的原子更新为准，并发操作先到先得，后到者影响行数 0 报错提示。
- **消息可靠性**：RabbitMQ 开启发送方确认（publisher-confirm，correlated）；延迟消息在事务提交后（afterCommit）发送，MQ 异常不影响下单；confirm nack 由线程池异步重试最多 3 次，仍失败写入 `oms_mq_send_fail` 记录表；未支付单的取消由 2 分钟扫表任务最终兜底（MQ 宕机/插件未装/进程崩溃均能关单还库存），因此不引入本地消息表。消费者手动 ACK，消费异常 basicNack（requeue=false）转入兜底死信队列 `order.timeout.dlq`，配合人工补偿。



### 5.3 安全

- 除登录、注册、刷新、登出、座位查询外的全部接口要求 **Access Token**（`Authorization: Bearer <token>`）。Refresh Token 只能调用 `/api/auth/refresh`。
- 登录签发双 Token：Access 2 小时、Refresh 7 天。Refresh 存在 Redis 白名单；登出将 Access `jti` 拉入 Redis 黑名单（TTL = 剩余寿命），与票面过期对齐。
- B 端密码以 **BCrypt** 哈希存储，不存明文。
- HTTP 拦截器在验签之外，对管理端路径（`/api/admin/**`、厨房看板、订单管理/撤销/出餐）强制 `role=ADMIN`，否则 403。前端路由守卫只做体验，真正权限在后端。
- WebSocket 握手同样强制鉴权：连接 URL 携带 `?token=`（须为未拉黑的 Access Token），厨房看板仅 ADMIN 可连，顾客端会话 userId 以 token 解析为准（不信任客户端自报）。
- JWT 载荷含 `userId`、`role`、`tokenType`、`jti`；顾客端数据查询强制按 `userId` 过滤，防越权。
- 密钥类配置（数据库、Redis、JWT Secret、微信 AppSecret、OSS、LLM Key）均通过环境变量注入，不入库不入库版本控制。



### 5.4 AI 成本控制

- 客服：语义缓存全量用户共享（相似度 ≥0.92 命中，仅缓存非动态回答），命中不调 LLM；未命中仅多一次低单价 embedding 调用。
- 预测：每个菜品每日仅 1 次 LLM 调用；定时任务凌晨低峰执行。
- RAG：检索截断 Top-3，控制上下文长度。

---



## 六、风险清单与应对


| #   | 风险                  | 等级  | 应对                                 |
| --- | ------------------- | --- | ---------------------------------- |
| 1   | 并发加菜/支付/撤销产生状态竞争    | 高   | 状态迁移 SQL 条件更新（影响行数裁决），非法迁移直接报错；支付唯一索引兜底 |
| 2   | Redis 与 MySQL 库存不一致 | 高   | Lua 原子操作 + 失败回滚脚本 + MySQL 条件更新二次校验 |
| 3   | 厨房看板断线漏单            | 中   | 心跳保活 + 自动重连 + 重连后 HTTP 快照全量补齐      |
| 4   | 支付与超时取消并发竞争        | 中   | 取消走 `status=0 AND pay_time IS NULL` 条件更新，与支付只成功一笔；支付胜出时超时消息 ACK 跳过      |
| 5   | LLM 输出 JSON 格式不稳定   | 中   | 超时保护 + 格式校验失败降级为时序预测结果             |
| 6   | 新品/新店无历史数据预测失真      | 中   | 五级冷启动降级链，最终兜底值保证流程不中断              |
| 7   | 天气/节假日外部 API 不可用    | 中   | 跳过该维度并在 Prompt 注明缺失，预测继续           |
| 8   | RabbitMQ 宕机导致超时不取消  | 低   | 发送方确认可感知失败并异步重试 3 次 + 落 `oms_mq_send_fail`；下单不受影响；2 分钟扫表兜底自动关单还库存       |
| 9   | 知识库版本碎片化            | 低   | 版本 + 状态 + 生效时间管理，检索按版本过滤           |


